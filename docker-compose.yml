services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ccoe
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["ccoe-data:/var/lib/postgresql/data"]
volumes:
  ccoe-data:
